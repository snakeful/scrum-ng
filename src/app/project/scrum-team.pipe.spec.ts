import { ScrumTeamPipe } from './scrum-team.pipe';

describe('ScrumTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new ScrumTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
