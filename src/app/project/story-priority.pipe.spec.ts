import { StoryPriorityPipe } from './story-priority.pipe';

describe('StoryPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new StoryPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
